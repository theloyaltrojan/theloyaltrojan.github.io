# theloyaltrojan.github.io

My GitHub user site. It serves two things:

- `ads.txt` at the host root, which is the only place ad crawlers look for it. The project site at
  [/hardwood](https://theloyaltrojan.github.io/hardwood/) is a subdirectory, so the copy in that repo
  is never read — this one is.
- `index.html`, a placeholder so the root is not a 404. Replace it whenever.
